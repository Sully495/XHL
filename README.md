# XHL Website - Fixed

This version fixes the broken standings/teams/stats sections caused by duplicate HTML IDs.

It also removes the large logo from the top-left navigation.

Edit:
- `index.html` = text/layout
- `style.css` = colors/design
- `script.js` = teams, standings, schedule and player stats
- `mountain-hero.jpg` = mountain homepage background
- `xhl-logo.jpeg` = XHL logo used in the footer

Upload all files directly into the root of your GitHub repository and commit the changes.
